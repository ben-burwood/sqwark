[project]
name = "sqwark"
version = "0.1"
description = "Feedback Aggregator"
requires-python = ">=3.13"
dependencies = [
    "robyn>=0.84.0",
    "slack-sdk>=3.0",
    "sqlalchemy>=2.0",
]

[tool.ruff]
line-length = 150
